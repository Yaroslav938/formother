streamlit>=1.30
anthropic>=0.39
pypdf>=4.0
python-docx>=1.1
pandas>=2.0
Pillow>=10.0
plotly>=5.18
numpy>=1.24

# --- Рендеринг сканов-PDF в изображение (для проверки PDF без текстового слоя) ---
# PyMuPDF не требует системных зависимостей.
PyMuPDF>=1.23

# --- OCR без системных пакетов (РАБОТАЕТ на Streamlit Cloud через pip) ---
# RapidOCR — основной движок, ставится только через pip, НЕ требует packages.txt.
# Поддерживает русский + английский. Это решает проблему «tesseract не ставится».
rapidocr-onnxruntime>=1.3

# --- Опциональные OCR-движки (запасные) ---
# pytesseract требует системный tesseract-ocr (см. packages.txt). Необязателен,
# если установлен rapidocr. Оставлен как fallback.
pytesseract>=0.3.10
# easyocr>=1.7      # альтернатива, тяжёлая (torch)
